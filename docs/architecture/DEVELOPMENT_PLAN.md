# Development Plan: 4 Phases (แผนการพัฒนา 4 ระยะ)

แผนการพัฒนานี้แบ่งออกเป็น 2 ส่วนหลัก: **Software** (Mobile App + Backend) และ **Hardware** (IoT Device)

## ภาพรวมความคืบหน้า (Progress Overview)

| ส่วน           | เสร็จแล้ว | ทั้งหมด |    สถานะ     |
| -------------- | :-------: | :-----: | :----------: |
| **Software**   |    22     |   28    | 🔄 **78.6%** |
| **Hardware**   |     2     |   33    | ⏳ **6.1%**  |
| **รวมทั้งหมด** |    24     |   61    |  **39.3%**   |

**Software (เสร็จแล้ว):** Auth, Elder CRUD, Emergency Call, Device Pairing, WiFi Config, Dashboard Real-time, Push Notification, Multi-User, Monthly Report, Notification History, Feedback, Admin Dashboard

**Hardware (เสร็จแล้ว):** ESP32 Wi-Fi + MQTT, AP Mode Configuration

**ยังไม่เสร็จ:** False Alarm (รอ Hardware), Phase 4 Testing, Hardware Sensors + Algorithm

---

# ส่วนที่ 1: แผนพัฒนา (Development Plan)

## Software Development Plan (แผนพัฒนาซอฟต์แวร์)

### ภาพรวมสำหรับนำเสนอ (Summary)

| Phase | ระยะ                              | ช่วงเวลา     |  สถานะ  |
| :---: | --------------------------------- | ------------ | :-----: |
|   1   | ฟังก์ชันหลัก (Core Functions)     | ก.ย.-พ.ย. 68 | ✅ 100% |
|   2   | ฟังก์ชันรอง (Secondary Functions) | พ.ย.-ธ.ค. 68 | ✅ 100% |
|   3   | ฟังก์ชันทั้งหมด (Full Functions)  | ธ.ค.-ม.ค. 68 | ✅ 100% |
|   4   | การทดสอบ (Testing & Deployment)   | ม.ค.-ก.พ. 69 |  ⏳ 0%  |

**สิ่งที่เหลือ (Phase 4):**

- **Sensor Integration:** ทดสอบข้อมูล HR/Fall จาก Hardware จริง ผ่าน MQTT → แสดงผลบน Dashboard
- **False Alarm:** ทดสอบปุ่มยกเลิกการแจ้งเตือนบนอุปกรณ์ ภายใน 30 วินาที
- **Demo:** สาธิตการทำงานให้อาจารย์/คณะกรรมการ

#### Phase 1: ฟังก์ชันหลัก (Core Functions)

**เป้าหมาย:** ผู้ใช้สามารถลงทะเบียน จัดการข้อมูลผู้สูงอายุ และโทรฉุกเฉินได้ (ก่อนเชื่อมต่อ IoT)

**ฟังก์ชัน (Caregiver):**

- ลงทะเบียน/เข้าสู่ระบบ (Register, Login)
- จัดการข้อมูลผู้สูงอายุ (Elder Profile)
- จัดการผู้ติดต่อฉุกเฉิน (Emergency Contacts)
- โทรฉุกเฉิน (Emergency Call)
- Dashboard เปล่า (Empty State)

**ฟังก์ชัน (Admin):**

- เข้าสู่ระบบ/ออกจากระบบ (Admin Login/Logout)
- ดูรายชื่อผู้ใช้งานและผู้สูงอายุ (User/Elder Management)

#### Phase 2: ฟังก์ชันรอง (Secondary Functions)

**เป้าหมาย:** ผู้ใช้สามารถจับคู่อุปกรณ์และติดตามสถานะ Online/Offline ได้

**ฟังก์ชัน (Caregiver):**

- เชื่อมต่ออุปกรณ์ (Device Pairing) ผ่าน QR Code
- Dashboard แสดงสถานะ Online/Offline
- ดูเหตุการณ์ย้อนหลัง (Event History)
- ระบบ Feedback/แจ้งซ่อม

**ฟังก์ชัน (Admin):**

- ลงทะเบียนอุปกรณ์และสร้าง QR Code (Register Device)
- ดูรายการ/ลบ/ยกเลิกการจับคู่อุปกรณ์ (Device Management)
- จัดการ Feedback/Repair Requests (อัปเดตสถานะ)
- Dashboard แสดงภาพรวมระบบ (System Overview)

#### Phase 3: ฟังก์ชันทั้งหมด (Full Functions)

**เป้าหมาย:** ผู้ใช้ได้รับข้อมูล Sensor แบบ Real-time และแจ้งเตือนเมื่อเกิดเหตุการณ์ผิดปกติ

**ฟังก์ชัน (Caregiver):**

- แสดง Heart Rate และ Fall Status แบบ Real-time
- Push Notifications (Fall, HR, Offline)
- เชิญสมาชิกครอบครัว (Multi-User Access)
- รายงานสุขภาพรายเดือน (Monthly Report)
- ดูประวัติการแจ้งเตือน (Notification History)

#### Phase 4: การทดสอบ (Testing & Deployment)

**เป้าหมาย:** ระบบทดสอบ Integration กับ Hardware จริงและพร้อมสาธิต

**ขั้นตอน:**

- System Integration Testing (Mobile + Backend + Hardware ทำงานร่วมกัน)
- Sensor Calibration (ปรับจูน Threshold ตรวจจับการล้ม)
- Demo Preparation (เตรียมสาธิตการทำงาน)
- Presentation (นำเสนออาจารย์/คณะกรรมการ)

---

## Hardware Development Plan (แผนพัฒนาฮาร์ดแวร์)

### ภาพรวมสำหรับนำเสนอ (Summary)

| Phase | ระยะ                              | ช่วงเวลา     | สถานะ  |
| :---: | --------------------------------- | ------------ | :----: |
|   1   | ฟังก์ชันหลัก (Core Functions)     | พ.ย. 68      | 🔄 33% |
|   2   | ฟังก์ชันรอง (Secondary Functions) | พ.ย.-ธ.ค. 68 | ⏳ 0%  |
|   3   | ฟังก์ชันทั้งหมด (Full Functions)  | ธ.ค.-ม.ค. 68 | ⏳ 0%  |
|   4   | การทดสอบ (Testing & Validation)   | ม.ค.-ก.พ. 69 | ⏳ 0%  |

**สิ่งที่เหลือแต่ละ Phase:**

**Phase 1 (33%):**

- อ่านค่า Accelerometer/Gyroscope จาก MPU6050 ผ่าน I²C
- คำนวณ SMV + Complementary Filter ประมาณมุม Pitch/Roll
- ปุ่มกดทำงานได้ ตรวจจับ HIGH/LOW ถูกต้อง

**Phase 2 (0%):**

- Pulse Sensor XD-58C อ่านค่า HR ได้ + กรองสัญญาณรบกวน
- Battery LiPo + TP4056 ชาร์จได้ + Step-Up 5V จ่ายไฟ ESP32
- Grove Speaker สร้างเสียงเตือนได้

**Phase 3 (0%):**

- Fall Detection Algorithm: ใช้ **SisFall Dataset** + จำลองล้มจริง 2-3 คน
- ส่ง JSON ผ่าน MQTT เมื่อเกิดเหตุการณ์
- **ประกอบ Prototype เสร็จ:** PCB/กล่อง + สายคล้องคอ

**Phase 4 (0%):**

- **ทดสอบความแม่นยำ:** เทียบผล SisFall + อาสาสมัครจริง
- ทดสอบ HR Accuracy เทียบกับอุปกรณ์มาตรฐาน
- ทดสอบ Battery Life: ใช้งานได้กี่ชั่วโมง/การชาร์จ

#### Phase 1: ฟังก์ชันหลัก (Core Functions)

**เป้าหมาย:** พัฒนาฮาร์ดแวร์พื้นฐานให้สามารถตรวจจับการเคลื่อนไหวและสื่อสารกับ Backend ได้

**ฟังก์ชัน:**

- ESP32 + MPU6050 อ่านค่า Accelerometer/Gyroscope
- เชื่อมต่อ Wi-Fi + MQTT
- รองรับ AP Mode สำหรับตั้งค่า

#### Phase 2: ฟังก์ชันรอง (Secondary Functions)

**เป้าหมาย:** เพิ่ม Sensor วัดชีพจรและระบบพลังงานแบบพกพา

**ฟังก์ชัน:**

- Pulse Sensor XD-58C วัดชีพจร
- LiPo Battery + TP4056 ชาร์จ
- Grove Speaker เสียงเตือน

#### Phase 3: ฟังก์ชันทั้งหมด (Full Functions)

**เป้าหมาย:** พัฒนา Algorithm ตรวจจับการล้มและระบบแจ้งเตือนครบถ้วน

**ฟังก์ชัน:**

- Fall Detection (Threshold-based)
- ส่ง JSON ผ่าน MQTT
- ออกแบบ PCB/กล่อง และสายคล้องคอ

#### Phase 4: การทดสอบ (Testing & Validation)

**เป้าหมาย:** ตรวจสอบความแม่นยำ ความเสถียร และความพร้อมใช้งานจริงของอุปกรณ์

**ขั้นตอน:**

- ทดสอบจำลองการล้ม (Forward/Backward/Side Fall)
- เทียบ HR Sensor กับอุปกรณ์มาตรฐาน
- ทดสอบอายุแบตเตอรี่

---

# ส่วนที่ 2: Checklist สำหรับติดตามงาน

## Software Development Tracker

### Phase 1: ฟังก์ชันหลัก (Core Functions)

**เป้าหมาย:** ผู้ใช้สามารถลงทะเบียน จัดการข้อมูลผู้สูงอายุ และโทรฉุกเฉินได้ (ก่อนเชื่อมต่อ IoT)

**Actor: ญาติผู้ดูแล (Caregiver):**

- [x] พัฒนาฟังก์ชันการลงทะเบียนบัญชีผู้ใช้ (Register) เข้าสู่ระบบ (Login) และจัดการข้อมูลส่วนตัว
- [x] พัฒนาฟังก์ชันการเพิ่ม ดู แก้ไข ข้อมูลผู้สูงอายุ (Elder Profile)
- [x] พัฒนาฟังก์ชันการจัดการรายชื่อผู้ติดต่อฉุกเฉิน (Emergency Contacts) เพิ่ม/ลบ/จัดลำดับ
- [x] พัฒนาฟังก์ชันโทรฉุกเฉิน (Emergency Call) รองรับทั้งการกดเองและผ่านการแจ้งเตือน
- [x] พัฒนาฟังก์ชันแสดงหน้า Dashboard เปล่า (Empty State) รองรับการเริ่มใช้งาน

**Actor: ผู้ดูแลระบบ (Admin):**

- [x] พัฒนาฟังก์ชันเข้าสู่ระบบ (Admin Login) และจัดการบัญชีผู้ดูแล
- [x] พัฒนาฟังก์ชันดูรายชื่อผู้ใช้งาน (User Management) และรายชื่อผู้สูงอายุ (Elder Management)

---

### Phase 2: ฟังก์ชันรอง (Secondary Functions)

**เป้าหมาย:** ผู้ใช้สามารถจับคู่อุปกรณ์และติดตามสถานะ Online/Offline ได้

**Actor: ญาติผู้ดูแล (Caregiver):**

- [x] พัฒนาฟังก์ชันเชื่อมต่ออุปกรณ์ (Device Pairing) ผ่าน QR Code และ Manual Entry
- [x] พัฒนาฟังก์ชันอัปเดตการตั้งค่า Wi-Fi ให้กับอุปกรณ์ (WiFi Update)
- [x] พัฒนาฟังก์ชัน Dashboard แสดงสถานะอุปกรณ์ Online/Offline แบบ Real-time
- [x] พัฒนาฟังก์ชันดูเหตุการณ์ย้อนหลัง (Event History) พร้อมตัวกรอง (25, 50, ทั้งหมด)
- [x] พัฒนาฟังก์ชันส่งความคิดเห็น (Feedback) หรือแจ้งซ่อม (Repair Request) พร้อมติดตามสถานะ

**Actor: ผู้ดูแลระบบ (Admin):**

- [x] พัฒนาฟังก์ชันลงทะเบียนอุปกรณ์เข้าระบบ (Register Device) และสร้าง QR Code
- [x] พัฒนาฟังก์ชันดูรายการอุปกรณ์ทั้งหมดและสถานะการเชื่อมต่อ
- [x] พัฒนาฟังก์ชันจัดการความคิดเห็น (General Feedback) และคำร้องขอแจ้งซ่อม (Repair Requests)

---

### Phase 3: ฟังก์ชันทั้งหมด (Full Functions)

**เป้าหมาย:** ผู้ใช้ได้รับข้อมูล Sensor แบบ Real-time และแจ้งเตือนเมื่อเกิดเหตุการณ์ผิดปกติ

**Actor: ผู้สูงอายุ (Elder):**

- [ ] พัฒนาฟังก์ชันการยกเลิกการแจ้งเตือน (False Alarm) ผ่านปุ่มบนอุปกรณ์

**Actor: ญาติผู้ดูแล (Caregiver):**

- [x] พัฒนาฟังก์ชันแสดงข้อมูลสุขภาพ Real-time (Heart Rate, Fall Status) บน Dashboard
- [x] พัฒนาฟังก์ชันการแจ้งเตือน (Push Notifications) สำหรับการหกล้ม, ชีพจรผิดปกติ, และอุปกรณ์ Offline
- [x] พัฒนาฟังก์ชันการเชิญสมาชิกครอบครัว (Multi-User Access) และกำหนดสิทธิ์ (Editor, Viewer)
- [x] พัฒนาฟังก์ชันรายงานสุขภาพรายเดือน (Monthly Health Report) และข้อมูลสรุป
- [x] พัฒนาฟังก์ชันดูประวัติการแจ้งเตือน (Notification History) จัดการสถานะและลบรายการได้
- [x] พัฒนาฟังก์ชันการลบบัญชีผู้ใช้ (Delete Account)

**Actor: ผู้ดูแลระบบ (Admin):**

- [x] พัฒนาฟังก์ชัน Dashboard แสดงข้อมูลสรุปภาพรวมของระบบ (System Overview)
  - แสดงจำนวนผู้ใช้งาน (Total Users) และผู้ใช้งานที่กำลังใช้งาน (Active Users)
  - แสดงจำนวนผู้สูงอายุ (Total Elders) และผู้สูงอายุที่เปิดใช้งาน (Active Elders)
  - แสดงจำนวนอุปกรณ์ (Active Devices) และสรุปจำนวนการหกล้มวันนี้ (Today's Falls)

---

### Phase 4: การทดสอบ (Testing & Deployment)

**เป้าหมาย:** ระบบทดสอบ Integration กับ Hardware จริงและพร้อมสาธิต

**Actor: ทีมพัฒนาและทดสอบ (Dev & QA Team):**

- [ ] **System Integration:** ทดสอบการทำงานร่วมกันทั้งระบบ (Mobile - Backend - Firmware)
- [ ] **Sensor Calibration:** ปรับจูนความแม่นยำในการตรวจจับการหกล้มและวัดชีพจร
- [ ] **Load Testing:** ทดสอบการรองรับการเชื่อมต่อพร้อมกันจำนวนมาก
- [ ] **User Acceptance Testing (UAT):** ทดสอบความพึงพอใจการใช้งานจริง
- [ ] **Deployment:** ติดตั้งระบบขึ้น Server จริงและปล่อยแอปพลิเคชัน

---

## Hardware Development Tracker

### Phase 1: ฟังก์ชันหลัก (Core Functions) - พ.ย. 2568

**เป้าหมาย:** พัฒนาฮาร์ดแวร์พื้นฐานให้สามารถตรวจจับการเคลื่อนไหวและสื่อสารกับ Backend ได้

**พื้นฐาน (ESP32 + MPU6050):**

- [ ] ESP32-DevKitC V4 สามารถอ่านค่า Accelerometer และ Gyroscope จาก MPU6050 ผ่าน I²C ได้
- [ ] เขียนโค้ดคำนวณค่า Signal Magnitude Vector (SMV) จากข้อมูล Accelerometer 3 แกน
- [ ] ใช้ Complementary Filter รวมสัญญาณจาก Accelerometer และ Gyroscope เพื่อประมาณมุม Pitch/Roll

**การเชื่อมต่อ (Connectivity):**

- [x] ESP32 เชื่อมต่อ Wi-Fi และส่งข้อมูลพื้นฐาน (เช่น "Hello World") ไปยัง MQTT Broker ได้สำเร็จ
- [x] รองรับ AP Mode สำหรับตั้งค่า Wi-Fi ผ่าน Mobile App

**ปฏิสัมพันธ์ (User Interaction):**

- [ ] ปุ่มกด (Large Push Button) ทำงานได้และตรวจจับสถานะ HIGH/LOW ได้ถูกต้อง

---

### Phase 2: ฟังก์ชันรอง (Secondary Functions) - พ.ย.-ธ.ค. 2568

**เป้าหมาย:** เพิ่ม Sensor วัดชีพจรและระบบพลังงานแบบพกพา

**Sensor เพิ่มเติม (Pulse Sensor XD-58C):**

- [ ] เพิ่ม Pulse Sensor (XD-58C) เชื่อมต่อผ่าน GPIO34 (Analog)
- [ ] เขียนโค้ดอ่านค่าสัญญาณ PPG และคำนวณอัตราการเต้นของหัวใจ (BPM)
- [ ] ใช้ Band-Pass Filter (0.5-5 Hz) กรองสัญญาณรบกวนจากการเคลื่อนไหว

**ระบบพลังงาน (Power System):**

- [ ] ประกอบวงจรพื้นฐานด้วยแบตเตอรี่ LiPo 3.7V 450mAh
- [ ] ติดตั้งโมดูลชาร์จ TP4056 พร้อมวงจรป้องกันการชาร์จเกินและคายประจุเกิน
- [ ] ใช้ Power Module (Step-Up Boost 3.7V → 5V) จ่ายไฟให้ ESP32

**การแจ้งเตือน (Alert System):**

- [ ] เพิ่มโมดูลลำโพง Grove Speaker สำหรับเสียงเตือน
- [ ] เขียนโค้ดสร้างเสียงเตือนเมื่อตรวจพบเหตุการณ์ผิดปกติ

---

### Phase 3: ฟังก์ชันทั้งหมด (Full Functions) - ธ.ค. 2568

**เป้าหมาย:** พัฒนา Algorithm ตรวจจับการล้มและระบบแจ้งเตือนครบถ้วน

**Algorithm ตรวจจับการล้ม (Fall Detection):**

- [ ] พัฒนาและปรับจูน Algorithm การตรวจจับการล้มโดยใช้ Threshold-based Analysis
- [ ] กำหนดค่า Threshold สำหรับ:
  - ค่าความเร่งรวม (SMV) ≈ 2.5g-3.2g สำหรับตรวจจับ Impact
  - อัตราการหมุน ≈ 250-300 °/s สำหรับตรวจจับการเสียการทรงตัว
  - ช่วงนิ่งหลัง Impact (Post-Fall Phase) 0.5-1 วินาที
- [ ] ใช้ Orientation Features (Pitch/Roll) ยืนยันท่าทางหลังการล้ม

**การแจ้งเตือน (MQTT Messaging):**

- [ ] เขียนโค้ดส่งข้อมูลที่มีโครงสร้าง JSON ไปยัง MQTT Broker เมื่อตรวจจับ:
  - การหกล้ม (Fall Detection)
  - ค่าชีพจรสูงกว่าเกณฑ์ (HR High > 120 BPM)
  - ค่าชีพจรต่ำกว่าเกณฑ์ (HR Low < 50 BPM)
- [ ] รองรับการยกเลิกการแจ้งเตือน (False Alarm) ผ่านปุ่มกดภายใน 30 วินาที

**การออกแบบ (Physical Design):**

- [ ] เริ่มออกแบบและประกอบอุปกรณ์ลงบนแผงวงจร PCB หรือกล่องที่เหมาะสม
- [ ] ติดตั้ง Slide Switch SS12D00 สำหรับเปิด/ปิดอุปกรณ์
- [ ] ออกแบบสายคล้องคอ (Neck Strap) และตำแหน่งติดตั้ง Easy Earclip สำหรับ Pulse Sensor

---

### Phase 4: การทดสอบ (Testing & Validation) - ม.ค. 2569

**เป้าหมาย:** ตรวจสอบความแม่นยำ ความเสถียร และความพร้อมใช้งานจริงของอุปกรณ์

**การจำลองสถานการณ์ (Scenario Simulation):**

- [ ] ทดสอบ Algorithm การตรวจจับการล้มในสถานการณ์จำลองต่างๆ ตามแผนการทดสอบ:
  - การล้มไปข้างหน้า (Forward Fall)
  - การล้มหงายหลัง (Backward Fall)
  - การล้มไปด้านข้าง (Side Fall)
- [ ] ใช้กลุ่มตัวอย่างอาสาสมัคร 2-3 คน ที่มีความหลากหลายทางกายภาพ (ส่วนสูง, น้ำหนัก, เพศ)

**ความแม่นยำ (Accuracy Testing):**

- [ ] ทดสอบความแม่นยำของ Heart Rate Sensor เทียบกับอุปกรณ์มาตรฐาน
- [ ] ทดสอบความถูกต้องของการตรวจจับการล้ม (Sensitivity/Specificity)
- [ ] ทดสอบการแยกแยะเหตุการณ์ล้มจริงออกจากกิจกรรมประจำวัน (ADL)

**ความเสถียร (Stability Testing):**

- [ ] ทดสอบการเชื่อมต่อและอายุการใช้งานของแบตเตอรี่ในการใช้งานต่อเนื่อง
- [ ] วัดประสิทธิภาพพลังงาน: ชั่วโมงการใช้งานต่อการชาร์จหนึ่งครั้ง
- [ ] ทดสอบ Auto-reconnect เมื่อ Wi-Fi หรือ MQTT ขาดการเชื่อมต่อ

**การตรวจสอบสุขภาพอุปกรณ์ (Device Health Check) [Future]:**

- [ ] (แผนในอนาคต) อุปกรณ์ทำ Self-Test ตรวจสอบการเชื่อมต่อ MPU6050 และ Pulse Sensor
- [ ] หากพบปัญหา ส่งสถานะ `{ "error": "SENSOR_FAIL" }` ผ่าน MQTT
- [ ] Mobile App แสดงสถานะ "Maintenance Needed" (สีส้ม) แทน "Online" (สีเขียว)

---

**Last Updated:** December 13, 2025
